#include<iostream>
#include<bits/stdc++.h>
using namespace std;

// p(x) = 3x^5 + 2x^4 + 5x^2 + 2x + 7

class term{
    public:
    int coeff, exp;
};

class poly{                      // polynomial = array of terms
    public:
    int n;
    term *t;
};

void create(poly *p){
    cout<<"Enter no of terms: ";
    cin>>p->n;
    int arr[n][n];
    for(int i=0; i<n; i++)1{
        for(int j=0; j<n; j++){
            
        }
    }
}

int main(){

    return 0;
}